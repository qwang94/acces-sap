{% extends 'base.html.twig' %}

{% block title %}Modifier une facture{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier une facture</h1>

        {{ include('invoice/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('home') }}">Retour à l'accueil</a>

        {{ include('invoice/_delete_form.html.twig') }}
    </div>
{% endblock %}
